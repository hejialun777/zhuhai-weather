# 依赖与构建产物
node_modules/
dist/
build/

# 日志、临时文件、运行时残留
*.log
*.tmp
*.pid
.DS_Store
Thumbs.db

# 编辑器与 IDE
.vscode/
.idea/

# 环境变量
# 本项目调用 Open-Meteo 开放接口，无需任何 API Key，
# 这里保留占位以防将来接入需要密钥的数据源时误传。
.env
.env.*
!.env.example

# 本地调试产物（截图等）
.shots/
